{
  "name": "hangar-sync-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Small webhook/sync server for Project Hangar desktop.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js"
  }
}
